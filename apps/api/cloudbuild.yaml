steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'apps/api/Dockerfile'
      - '-t'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/elimubora/api'
      - '.'
images:
  - 'us-central1-docker.pkg.dev/${PROJECT_ID}/elimubora/api'
